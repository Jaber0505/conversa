server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # ---------- Health check (silencieux) ----------
  location = /healthz {
    access_log off;
    default_type text/plain;
    add_header Cache-Control "no-store, no-cache, must-revalidate" always;
    return 200 "ok";
  }

  # ---------- PWA (si présent) ----------
  # Angular PWA: ne jamais mettre ngsw.json en cache
  location = /ngsw.json {
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    add_header Pragma "no-cache" always;
    add_header Expires "0" always;
    try_files $uri =404;
  }

  # ---------- HTML (index et autres) ----------
  # index.html : no-cache pour permettre les mises à jour
  location = /index.html {
    add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    add_header Pragma "no-cache" always;
    add_header Expires "0" always;
    try_files $uri =404;
  }

  # Tous les .html : no-cache (si d'autres HTML existent)
  location ~* \.html$ {
    add_header Cache-Control "no-c_
